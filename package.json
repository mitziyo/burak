{
  "name": "burak",
  "version": "1.0.0",
  "description": "This is burak project backend",
  "main": "src/server.ts",
  "scripts": {
    "start": "ts-node src/server.ts",
    "start:dev": "nodemon --exec ts-node src/server.ts",
    "build": "tsc",
    "traints": "nodemon --exec ts-node src/train.ts",
    "train": "nodemon --exec python3 train.py",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "MIT",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.0",
    "moment": "^2.29.4",
    "mongodb": "^3.2.6",
    "mongoose": "^6.2.9"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.2.0",
    "tsx": "^4.22.3",
    "typescript": "^5.2.2"
  }
}
